= TiniTracker API 문서
:doctype: book
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 2

== 다이어리 API

=== 다이어리 생성

`POST /diary`

operation::diary-create[snippets='http-request,request-fields,http-response,response-fields']

=== 다이어리 조회

`GET /diary/{diaryId}`

operation::diary-get[snippets='http-request,path-parameters,http-response,response-fields']

=== 다이어리 수정

`PATCH /diary/{diaryId}`

operation::diary-update[snippets='http-request,path-parameters,request-fields,http-response,response-fields']

=== 사용자별 다이어리 목록 조회

`GET /diary/user/{userUuid}`

operation::diary-get-by-user[snippets='http-request,path-parameters,http-response,response-fields']